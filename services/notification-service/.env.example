SPRING_PROFILES_ACTIVE=dev
SERVICE_NAME=notification-service
SERVER_PORT=8086
APP_TIMEZONE=UTC
TZ=UTC

DB_HOST=localhost
DB_PORT=5432
DB_NAME=tickefy
DB_USERNAME=tickefy
DB_PASSWORD=change_me
DB_SCHEMA=notification_service

REDIS_HOST=localhost
REDIS_PORT=6379

RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ_USERNAME=tickefy
RABBITMQ_PASSWORD=change_me

# Mail (Mailpit for dev, SendGrid/SMTP for prod)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=false
MAIL_STARTTLS=false
MAIL_FROM=noreply@tickefy.vn
MAIL_FROM_NAME=Tickefy

# JWT — path to RSA public key PEM file used to verify Bearer tokens (RS256).
# Dev: classpath:keys/public.pem (placeholder — replace with actual key from auth-service).
# Prod: mount the real PEM via secret and point here.
JWT_PUBLIC_KEY_LOCATION=classpath:keys/public.pem

LOG_LEVEL=INFO
